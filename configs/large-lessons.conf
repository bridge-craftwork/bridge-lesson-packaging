# Profile: large lessons (many boards each) — slice into 4/5/6-board sets.
# Declarer's plan gated to play-oriented categories (bidding-hand play is automatic, and the
# plan is the largest artifact — see CONTRACT.md and bridge-wrangler PDF-size notes).
INPUT_DIR="input"                 # a tree of {category}/{lesson}/ clean lesson PBNs
OUTPUT_DIR="output"
SET_SIZES="4 5 6"
ROTATE_PATTERNS="S,NS,NESW"
DECLARER_PLAN_CATEGORY="Declarer Play"   # match your play category; "" = every lesson
LIN="0"
